name: Docker Image CI

on:
  push:
    branches: [ "aws-multi-instance" ]
  pull_request:
    branches: [ "aws-multi-instance" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file azure/Dockerfile --tag my-image-name:$(date +%s)
